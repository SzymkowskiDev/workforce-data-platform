nowy branch (name: data gen..)
timeline: 1 sprint do 25.03

tasks:
generacja csv
dynamincze genenrowanie sql
parametry od data science teamu


używamy wbudowanych błędów czy tworzymy własne?


GIT: (git ...)

init -> new local repo

clone <repo_https> -> clone to cwd
pull -> update check

add -A -> add all, files are pushed to staging area
commit -m <desc> -> 
push -> push local image to GH

checkout -> change branch

reset --hard [sha=last] -> go back to unchanged form

pull origin master -> update from master

log -> commits history
revert <id> -> restore <id> state image

branch -> show all branches
status -> staging area (what will be commited)
show

